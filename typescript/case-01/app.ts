const ageValue:number = 25;
const nameValue:string = 'Valery';

const user = {
    age:number = 25,
    name:string = 'Valery'
};

const arr = [ 'uno', 'duo', 'tre' ];

// ------------------------------------------
for ( let val of arr ) {
    console.log( val );
}

console.log( user.name );