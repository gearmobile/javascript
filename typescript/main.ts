var food: string = 'melon';
var quantity: number = 300;
var tasty: boolean - true;

function speak( primo: string, secondo: number ): void {
    console.log( 'We have ' + secondo + ' ' + primo + 's' );
}

speak( food, quantity );
